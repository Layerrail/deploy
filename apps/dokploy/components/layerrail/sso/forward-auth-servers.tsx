import { CardDescription, CardHeader, CardTitle } from "@/components/ui/card";

export const ForwardAuthServers = () => (
	<CardHeader>
		<CardTitle>Application authentication</CardTitle>
		<CardDescription>
			Forward-auth policies will be managed by LayerRail Deploy.
		</CardDescription>
	</CardHeader>
);
