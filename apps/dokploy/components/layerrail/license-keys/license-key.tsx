import { CardDescription, CardHeader, CardTitle } from "@/components/ui/card";

export const LicenseKeySettings = () => (
	<CardHeader>
		<CardTitle>LayerRail Deploy</CardTitle>
		<CardDescription>
			This build uses LayerRail licensing and billing instead of LayerRail Deploy license keys.
		</CardDescription>
	</CardHeader>
);
